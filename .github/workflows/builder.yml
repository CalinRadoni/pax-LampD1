name: Build with ESP-IDF v4.1

on: [push]

jobs:
  builder:
    name: Builder for the test project
    runs-on: ubuntu-20.04

    env:
      esp-idf-ver: v4.1

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          path: ~/main

      - uses: actions/checkout@v2
        with:
          repository: CalinRadoni/ESP32BoardManager
          path: ~/CodeLib/ESP32/ESP32BoardManager

      - uses: actions/checkout@v2
        with:
          repository: CalinRadoni/ESP32DLEDController
          path: ~/CodeLib/ESP32/ESP32DLEDController

      - uses: actions/checkout@v2
        with:
          repository: CalinRadoni/ESP32HAL
          path: ~/CodeLib/ESP32/ESP32HAL

      - uses: actions/checkout@v2
        with:
          repository: CalinRadoni/ESP32RMT
          path: ~/CodeLib/ESP32/ESP32RMT

      - uses: actions/checkout@v2
        with:
          repository: CalinRadoni/ESP32SimpleOTA
          path: ~/CodeLib/ESP32/ESP32SimpleOTA

      - uses: actions/checkout@v2
        with:
          repository: CalinRadoni/ESP32Timers
          path: ~/CodeLib/ESP32/ESP32Timers

      - uses: actions/checkout@v2
        with:
          repository: CalinRadoni/Debouncer
          path: ~/CodeLib/Generic/Debouncer

      - name: Cache the ESP-IDF
        id: cache-esp-idf
        uses: actions/cache@v2
        with:
          path: |
            ~/esp
            ~/.espressif
          key: ${{ runner.os }}-${{ env.esp-idf-ver }}

      - name: Install ESP-IDF
        if: steps.cache-esp-idf.outputs.cache-hit != 'true'
        uses: CalinRadoni/esp-idf-v4-action@v2
        with:
          esp_idf_version: ${{ env.esp-idf-ver }}

      - name: Build
        run: |
          export CodeLib_PATH="${HOME}/CodeLibrary"
          source ~/esp/esp-idf/export.sh
          cd "${HOME}/main/SW"
          idf.py --version
          idf.py reconfigure
          idf.py app
          idf.py size
